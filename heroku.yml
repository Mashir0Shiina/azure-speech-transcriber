setup:
  addons:
    - plan: heroku-redis
      as: REDIS

build:
  docker:
    web: Dockerfile
    worker: Dockerfile
  config:
    FLASK_APP: app.py
    PYTHONPATH: /app

run:
  web: flask run --host=0.0.0.0
  worker: celery -A app.celery_app worker --loglevel=info 